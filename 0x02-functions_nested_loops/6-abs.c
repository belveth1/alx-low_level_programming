#include "main.h"
/**
 *_abs - function that computes the absolute value number
 *@n: the number is to be checked
 *Return: n the abs value
 */
int _abs(int n)
{
if (n < 0)
{
n = n* (-1);
}
return (n);
}
