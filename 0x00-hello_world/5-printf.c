#include <stdio.h>
/**
 * main - A program prints line by using (printf) function
 * Return: 0 (success)
 */
int main(void)
{
printf("with proper grammar, but the outcome is a piece of art,\n");
return (0);
