#include <stdio.h>
/**
 * main - is afunction that makes program(puts) function to print line
 * Return: (0) is (success)
 */
int main(void)
{
puts("\"Programming is like building a multilingual puzzle");
return (0);
}
